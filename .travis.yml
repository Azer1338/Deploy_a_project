language: python
python:
          - '3.5'

# safelist
branches:
    only:
     - production

before_script:
          - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="pur_beurre__project.settings"

services:
          - postgresql

script:
          - ./manage.py test
